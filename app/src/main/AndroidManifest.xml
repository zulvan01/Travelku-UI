<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dicoding.assosiate.travelku">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".BelandaActivity" />
        <activity android:name=".AustraliaActivity" />
        <activity android:name=".NewYorkActivity" />
        <activity android:name=".SingapuraActivity" />
        <activity android:name=".NotifikasiActivity" />
        <activity
            android:name=".LoadingActivity"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:theme="@style/FullScreen" />
        <activity android:name=".IndonesiaActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>